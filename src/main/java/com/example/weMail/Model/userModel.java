package com.example.weMail.Model;

public class userModel {
	/*
	`userNo`         INT             NOT NULL    AUTO_INCREMENT COMMENT 'user_no', 
    `user_id`        VARCHAR(45)     NOT NULL    COMMENT 'user_id', 
    `user_password`  VARCHAR(255)    NOT NULL    COMMENT 'user_password', 
    `user_name`      VARCHAR(45)     NOT NULL    COMMENT 'user_name', 
    `dept_no`        INT             NOT NULL    COMMENT 'dept_no', 
    `position_no`    INT             NOT NULL    COMMENT 'position_no', 
    `slack_info`     VARCHAR(300)    NULL        COMMENT 'slack_info', 
    `phone_num`      VARCHAR(60)     NOT NULL    COMMENT 'phone_num', 
    `gender`         char(10)        NOT NULL    COMMENT 'gender', 
    PRIMARY KEY (userNo)
	 */
}
